name: Deploy

on:
  push:
    branches:
      - main

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v2

      - name: Setup Node
        uses: actions/setup-node@v3
        with:
          node-version: 16

      - name: Install Vite globally
        run: sudo npm install -g vite

      - name: Install frontend dependencies
        run: cd frontend && npm install react-router-dom vite

      - name: Reinstall frontend dependencies
        run: |
          cd frontend
          rm -rf node_modules/
          npm install react-router-dom vite

      - name: Reinstall backend dependencies
        run: |
          cd backend
          rm -rf node_modules/
          npm install
          npm install --save-dev @types/node

      - name: Build frontend
        run: cd frontend && npx vite build

      - name: Install backend dependencies
        run: |
          cd backend && npm install
          npm install --save-dev @types/node

      - name: Build backend
        run: cd backend && tsc

      - name: Create .nojekyll file
        run: touch frontend/dist/.nojekyll

      - name: Deploy to GitHub Pages
        uses: JamesIves/github-pages-deploy-action@4.1.5
        with:
          branch: gh-pages
          folder: frontend/dist
